-- This file has been generated by EWG. Do not edit. Changes will be lost!

class CGDATA_PROVIDER_CALLBACKS_STRUCT

inherit

	EWG_STRUCT

	CGDATA_PROVIDER_CALLBACKS_STRUCT_EXTERNAL
		export
			{NONE} all
		end

create

	make_new_unshared,
	make_new_shared,
	make_unshared,
	make_shared

feature {NONE} -- Implementation

	sizeof: INTEGER is
		do
			Result := sizeof_external
		end

feature {ANY} -- Member Access

	get_getbytes: POINTER is
		obsolete "Use `getbytes' instead."
			-- Access member `getBytes'
		require
			exists: exists
		do
			Result := get_getbytes_external (item)
		ensure
			result_correct: Result = get_getbytes_external (item)
		end

	getbytes: POINTER is
			-- Access member `getBytes'
		require
			exists: exists
		do
			Result := get_getbytes_external (item)
		ensure
			result_correct: Result = get_getbytes_external (item)
		end

	set_getbytes (a_value: POINTER) is
			-- Set member `getBytes'
		require
			exists: exists
		do
			set_getbytes_external (item, a_value)
		ensure
			a_value_set: a_value = getbytes
		end

-- TODO: function pointers not yet callable from
--		struct, use corresponding callback class instead
	get_skipbytes: POINTER is
		obsolete "Use `skipbytes' instead."
			-- Access member `skipBytes'
		require
			exists: exists
		do
			Result := get_skipbytes_external (item)
		ensure
			result_correct: Result = get_skipbytes_external (item)
		end

	skipbytes: POINTER is
			-- Access member `skipBytes'
		require
			exists: exists
		do
			Result := get_skipbytes_external (item)
		ensure
			result_correct: Result = get_skipbytes_external (item)
		end

	set_skipbytes (a_value: POINTER) is
			-- Set member `skipBytes'
		require
			exists: exists
		do
			set_skipbytes_external (item, a_value)
		ensure
			a_value_set: a_value = skipbytes
		end

-- TODO: function pointers not yet callable from
--		struct, use corresponding callback class instead
	get_rewind: POINTER is
		obsolete "Use `rewind' instead."
			-- Access member `rewind'
		require
			exists: exists
		do
			Result := get_rewind_external (item)
		ensure
			result_correct: Result = get_rewind_external (item)
		end

	rewind: POINTER is
			-- Access member `rewind'
		require
			exists: exists
		do
			Result := get_rewind_external (item)
		ensure
			result_correct: Result = get_rewind_external (item)
		end

	set_rewind (a_value: POINTER) is
			-- Set member `rewind'
		require
			exists: exists
		do
			set_rewind_external (item, a_value)
		ensure
			a_value_set: a_value = rewind
		end

-- TODO: function pointers not yet callable from
--		struct, use corresponding callback class instead
	get_releaseprovider: POINTER is
		obsolete "Use `releaseprovider' instead."
			-- Access member `releaseProvider'
		require
			exists: exists
		do
			Result := get_releaseprovider_external (item)
		ensure
			result_correct: Result = get_releaseprovider_external (item)
		end

	releaseprovider: POINTER is
			-- Access member `releaseProvider'
		require
			exists: exists
		do
			Result := get_releaseprovider_external (item)
		ensure
			result_correct: Result = get_releaseprovider_external (item)
		end

	set_releaseprovider (a_value: POINTER) is
			-- Set member `releaseProvider'
		require
			exists: exists
		do
			set_releaseprovider_external (item, a_value)
		ensure
			a_value_set: a_value = releaseprovider
		end

-- TODO: function pointers not yet callable from
--		struct, use corresponding callback class instead
end
